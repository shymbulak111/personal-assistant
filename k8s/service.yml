apiVersion: v1
kind: Service
metadata:
  name: personal-assistant-service
spec:
  selector:
    app: personal-assistant
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
